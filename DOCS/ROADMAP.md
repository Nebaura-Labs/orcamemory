# Roadmap

Short-term (next 1–2 releases)
- Agent health endpoint + live connection status
- Usage metering enforcement (tokens, searches) per plan
- API key rotation + revoke flow
- Memory retention enforcement UI in project settings

Memory & search upgrades
- Embeddings pipeline (hybrid semantic + recency + tags)
- Automatic memory summarization for long-running agents
- Multi-memory views (current, episodic, long-term)
- Memory pinning and importance scoring

Agent experience
- OpenClaw plugin auto-sync for profile and session metadata
- Agent-side tooling for memory inspection and cleanup
- Structured memory types for tasks, preferences, and plans

Analytics & observability
- Session timelines with model + token usage summaries
- Recall effectiveness reports (hit rate, latency)
- Exportable memory audit logs

Platform & billing
- Plan upgrades/downgrades with proration
- Per-project agent limits and add-on seats
- Team invites + roles for workspace ownership

Integrations
- Webhook support for memory events
- SDKs for agent frameworks beyond OpenClaw
- Optional connectors for CRMs and internal docs
